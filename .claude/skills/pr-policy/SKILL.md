---
name: pr-policy
description: |
  Pull Request（PR）がリポジトリのルールに従っているかを検査します。
  同じ `pr-policy` スキルを `.github/skills` と `.claude/skills` の両方に置くことで、Copilot と Claude Code の両方から参照できるようにします。
---

## 同期
`.github/skills/pr-policy/SKILL.md` と内容を一致させてください。